version: "3.9"

services:
  app:
    build: .
    image: demo_project_app
    container_name: demo_project_app
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - APP_ENV=prod
    command: ["php", "-S", "0.0.0.0:8000", "-t", "public"]
